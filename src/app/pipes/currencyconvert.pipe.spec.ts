import { CurrencyconvertPipe } from './currencyconvert.pipe';

describe('CurrencyconvertPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyconvertPipe();
    expect(pipe).toBeTruthy();
  });
});
